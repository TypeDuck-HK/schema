# Rime configuration
# encoding: utf-8

enable_correction:
  enable_correction:
    translator/enable_correction: true

separate_candidates:
  separate_candidates:
    translator/combine_candidates: false

use_cangjie3:
  use_cangjie3:
    dictionary: cangjie3
    tips: 〔倉頡三代〕

__patch: []
